<!DOCTYPE html>
<html>
<head>
  <title>WhatzcookinRails</title>
  <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <nav class='navbar navbar-default'>
      <% if user_signed_in? %>
      Signed in as <%= current_user.name %>. Not you?
      <%= link_to "Sign out", destroy_user_session_path,:method => :delete %>
        <% if current_user.nutritional_profile %>
      <%= link_to "View Your Profile Page", user_nutritional_profiles_path(current_user,current_user.nutritional_profile.id) %>
        <% else %>
        <%= link_to "View Your Profile Page", user_nutritional_profiles_path(current_user), :method => :post %>
        <% end %>
      <% else %>
      <%= link_to "Sign up", new_user_registration_path %> or
      <%= link_to "Sign in", new_user_session_path %>
      <%= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2) %>

      <% end %>
    </nav>
  </header>
<div class='container'>
  <%= yield %>
</div>

</body>
</html>
